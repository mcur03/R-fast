import { Request, Response } from 'express';

class LoginController{
    // inicion de sesión de las personals
       static async loginPersona(req:Request, res:Response){
            const { tipoId, nomeroid, fechanac } = req.body;
            try {
                await 
            } catch (error) {
                
            }
       }
    }
    
